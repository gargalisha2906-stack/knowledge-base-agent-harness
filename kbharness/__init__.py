"""kbharness - a reusable knowledge-base agent harness for customer support.

The package is deliberately small and plain-Python so every piece can be
read end to end. Start at kbharness/agent.py (the pipeline entry point) and
follow the imports.
"""

__version__ = "0.1.0"
